{
  "type": "object",
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["clean", "findings"],
      "description": "\"findings\" if there is anything the CTO should answer before this lands; \"clean\" only if there is nothing at all."
    },
    "review": {
      "type": "string",
      "description": "The review itself, in markdown, addressed to the CTO."
    }
  },
  "required": ["verdict", "review"],
  "additionalProperties": false
}
